{% extends 'base.html.twig' %}

{% block title %}JobOffer{% endblock %}

{% block body %}
    <h1>JobOffer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_offer.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ job_offer.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ job_offer.description }}</td>
            </tr>
            <tr>
                <th>Details</th>
                <td>{{ job_offer.details }}</td>
            </tr>
            <tr>
                <th>JobType</th>
                <td>{{ job_offer.jobType }}</td>
            </tr>
            <tr>
                <th>PublicationDate</th>
                <td>{{ job_offer.publicationDate ? job_offer.publicationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('job_offer_index') }}">back to list</a>

    <a href="{{ path('job_offer_edit', {'id': job_offer.id}) }}">edit</a>

    {{ include('job_offer/_delete_form.html.twig') }}
{% endblock %}
