{% extends 'base.html.twig' %}

{% block title %}CustomerRequest{% endblock %}

{% block body %}
    <h1>CustomerRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ customer_request.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ customer_request.description }}</td>
            </tr>
            <tr>
                <th>ImageList</th>
                <td>{{ customer_request.imageList }}</td>
            </tr>
            <tr>
                <th>RequestState</th>
                <td>{{ customer_request.requestState }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('customer_request_index') }}">back to list</a>

    <a href="{{ path('customer_request_edit', {'id': customer_request.id}) }}">edit</a>

    {{ include('customer_request/_delete_form.html.twig') }}
{% endblock %}
