{% extends 'base.html.twig' %}

{% block title %}Galerie{% endblock %}

{% block body %}
    <h1>Galerie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ galerie.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ galerie.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ galerie.description }}</td>
            </tr>
            <tr>
                <th>ImageList</th>
                <td>{{ galerie.imageList }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('galerie_index') }}">back to list</a>

    <a href="{{ path('galerie_edit', {'id': galerie.id}) }}">edit</a>

    {{ include('galerie/_delete_form.html.twig') }}
{% endblock %}
